#!/usr/bin/env sh
# ★「根治」語ガード(2026-08-18 に配線。スクリプト自体は 2026-08-04 から在ったが
#   どのフックからも呼ばれておらず【一度も動いていなかった】= 判定はあるが未配線の型)。
#
# なぜ要るか(docs/handoff/ROOT-CAUSE-CLAIM-RULE.md):
#   90日で164回「根治」を宣言し、同じ症状が41回再発している。
#   「実機で確かめてから根治と言う」という規律は文書に在ったが、注意力頼みでは破られた。
#
# 何を止めるか: 「根治/真因を特定/完全に直/解決しました/直りました」と書いていて、
#   【修正後に症状が消えたことを示す語】が無いコミット。
#   ★修正【前】の実測を引用しただけでは通さない(原因の説明 ≠ 直った証拠)。
#
# 通し方: 実機確認したなら「反映後に確認/症状消失」等+実測値を書く。
#         まだなら「未確認/仮説/効くはず」と正直に書く(それなら通る)。
# 緊急回避: SKIP_ROOT_CAUSE_GATE=1 git commit ...
if [ -z "$SKIP_ROOT_CAUSE_GATE" ]; then
  node scripts/check-root-cause-claim.mjs "$1" || exit 1
fi
