node_modules/
test-results/
playwright-report/
blob-report/

# ローカル調査・生成物（誤って add しない）
docs/.visual-explainer/
.cursor/debug-*.log
nicolivelog-marketing-*.html
nicolivelog-extension-investigation.zip

# Codex/外部CLI ハンドオフ用の一時プロンプト・動画フレーム抽出（配布物でない）
.codex-exec-prompt-*.txt
.codex-task-prompt.txt
tmp-video-frames/
.artifacts/

# ユーザーがバックアップ zip を展開する作業用フォルダ（誤コミット防止）
新しいフォルダー/
新しいフォルダー */

# Chrome Web Store 申請用ビルド成果物
build/

# dev ホットリロード用シグナル（build:watch が生成・配布物には含めない）
extension/dist/dev-reload-id.txt

# API キー・シークレット（誤コミット防止 / 2026-05-17 nl-keys.txt 事故対策）
nl-keys.txt
keys.txt
*-keys.txt
*-keys.json
.env
.env.*
*.pem
*.key
secrets.json
credentials.json
.webui_secret_key

# CWS Publish API 認証情報(絶対にコミットしない)
.cws-credentials.json

# 一時作業ディレクトリ・散乱ファイル防止
tmp-video-frames/
新しいフォルダー/
新しいフォルダー */
test-results/
playwright-report/

.vercel
.env*.local

# Council Scout の状態ファイル（日々変わるスナップショット。日報 briefs/ はコミット対象）
council-scout/state.json
council-scout/LATEST.md

# 会議ハーネス(council-fable)の作業ログ。設問・各モデルの回答・実行ログの一時ファイルで、
# 結論は設計文書(docs/handoff/*-DESIGN.md)に転記済み。ルート直下に溜まり続けると
# tree-map:check が「ルート直下の設定ファイル」件数の drift で常時赤になる(2026-07-31)。
/council-*.json
/council-*.txt
/council-*.log
/_council-*.json

# 会議の自動記録(2026-08-25新設)。meeting.mjs が --out の有無に関わらずここへ1件書く。
# ★消さないこと: council-scout の「実会議の成績」がこのディレクトリを読んで
#   メンバーの実稼働を評価している(掃除スクリプトの対象にしない)。
# 追跡しない理由: council/ 自体はgit追跡下(371ファイル)なので、除外しないと
#   会議のたびに未コミット差分が出る。90日超は meeting.mjs 側が自動削除する。
council/auto/
/clip-history-persistence-*.json
/clip-history-persistence-*.txt
/winv-clipboard-freeze-*.json
/winv-clipboard-freeze-*.txt
/*-answers.json
/*-council.json
/*-council[0-9].json
/*-council-result.json
/login-speed-*.txt
/maximize-*.txt
/snippet-manager-*.txt
/venue_log.txt
