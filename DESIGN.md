---
version: alpha
name: Tsuioku Warm Pastel
description: >
  君斗りんくの追憶のきらめき — 3キャラ(りんく/こん太/たぬ姉)のゆっくり世界観に合う、
  明るい暖色パステルのビジュアルアイデンティティ。配信者を応援する温かさを、クリーム地・
  桜ピンク・やわらかオレンジで表す。AIっぽい無機質な青グレーや真っ黒は使わない。
colors:
  # 地と面 ── クリームの暖色ベース。真っ白(#fff)は使わず、わずかに温度を持たせる。
  bg: "#fff7ec"          # ページ地。温かいクリーム。
  bgSoft: "#fff1f4"      # 桜寄りの淡いピンク地(グラデ終端・補助面)。
  surface: "#fffdf9"     # カード/レーンの面。クリーム白(純白を避ける)。
  cardBg: "#fffdf9"      # surface の別名(既存トークン互換)。
  # 文字 ── 黒ではなく温かい焦げ茶系。コントラストは確保しつつ柔らかく。
  text: "#4a3b34"        # 本文。暖かいダークブラウン(#1f2937 の冷たい黒を置換)。
  textSoft: "#8a7568"    # 補助文。ベージュブラウン。
  textSub: "#7a6458"     # ラベル等の弱い文字。
  muted: "#9c8678"       # さらに弱い注記。
  # 枠 ── 地に溶けるクリームベージュの境界。グレーの硬い線を避ける。
  border: "#f0e0d2"
  # アクセント ── 桜ピンクを主役に、やわらかオレンジを副に。
  accent: "#f06aa0"      # 主アクセント(桜ピンク)。ボタン/リンク/強調。
  accentSoft: "#ffd9e6"  # アクセントの淡色(ホバー地など)。
  userAccent: "#f06aa0"  # 応援レーン強調(popup 互換・固定)。
  # 状態色 ── パステル寄りに彩度を落とし、地に馴染ませる。
  ok: "#3fa66a"          # 健全(緑)。
  processing: "#3a90c8"  # 処理中(青)。
  warn: "#d99a2b"        # 注目(オレンジ)。
  bad: "#e15a76"         # 要対応(やわらかい赤)。
typography:
  body:
    fontFamily: >
      -apple-system, BlinkMacSystemFont, 'Segoe UI', 'Hiragino Sans', 'Yu Gothic', sans-serif
    fontSize: 14px
    lineHeight: 1.5
  mono:
    fontFamily: ui-monospace, 'SFMono-Regular', 'Menlo', 'Consolas', monospace
rounded:
  sm: 8px      # 小要素(ボタン/チップ)。
  md: 12px     # カード/レーン。
  lg: 16px     # 大きな囲み(みちしるべ/共有ヒーロー)。
  pill: 999px  # 丸タイル/ピル。
spacing:
  xs: 4px
  sm: 8px
  md: 12px
  lg: 16px
elevation:
  # 影は黒ではなく暖色の影(焦げ茶を薄く)。浮きすぎない柔らかさ。
  card: "0 1px 6px rgba(120, 80, 50, 0.08)"
  raised: "0 4px 16px rgba(120, 80, 50, 0.12)"
---

## Overview

明るくて、手触りのある温かさ。これが追憶のきらめきのUIの核です。配信を応援するページなので、
冷たい SaaS ダッシュボードの青グレーや、生成AIが吐きがちな真っ黒+原色アクセントは使いません。
3キャラ(ゆっくり)のクリーム・桜ピンクの世界に、診断ページ(status)も自然に溶け込ませます。

設計の原則:
- **純白(#ffffff)と純黒は使わない。** 地はクリーム、文字は焦げ茶。常にわずかに温度を持たせる。
- **アクセントは桜ピンク1色を主役に。** 副にやわらかオレンジ。色を増やしすぎない。
- **影と枠は黒ではなく暖色。** 境界は地に溶けるベージュ、影は薄い焦げ茶。
- **レイアウトは変えない。** これはトークン(色・角丸・影・余白)の正本であり、既存の DOM 構造・
  情報設計はそのまま。読みやすさ・診断としての機能を損なわない範囲で「明るく良い感じ」にする。

## Colors

パレットはクリームの暖色を土台に、高すぎないコントラストで目に優しくしています。

- `bg` `surface` はクリーム。純白を避けることで画面全体が柔らかく、長時間見ても疲れにくい。
- `text` は焦げ茶(#4a3b34)。黒(#000 / #1f2937)より柔らかいが、クリーム地に対して十分な
  コントラスト(WCAG AA 本文相当)を確保。
- `accent` の桜ピンク(#f06aa0)は、既存の #ec4899 をわずかに明るく・暖かくしたもの。ゆっくりの
  頬の色に近づけ、「応援」の温度感を出す。
- 状態色(ok/warn/bad)はパステル寄りに彩度を落とし、クリーム地から浮かず、しかし識別できる強さを残す。

## Typography

本文は OS 標準のサンセリフ(日本語は Hiragino / Yu Gothic)。等幅(mono)は数値・診断ログ・
共有テキストなど「ズレない方が読みやすい」箇所だけに使う。フォント自体は変えず、色とサイズで温度を出す。

## Layout

余白は 4px グリッド(xs=4 / sm=8 / md=12 / lg=16)。カードとレーンは `rounded.md`(12px)、
ボタンやチップは `rounded.sm`(8px)、丸い顔タイルは `pill`。詰めすぎず、息のある間隔で「明るさ=余白」も作る。

## Elevation & Depth

影は黒ではなく暖色(焦げ茶を薄く)。`elevation.card` をカードの既定に、操作対象(共有ヒーロー等)だけ
`elevation.raised` で少し持ち上げる。浮かせすぎないのがパステルの上品さ。

## Shapes

角は基本まるい(8〜16px)。直角・鋭角は避け、ゆっくり世界の丸っこさに合わせる。顔アバターは完全な円(pill)。

## Do's and Don'ts

- ✅ クリーム地・焦げ茶文字・桜ピンクのアクセントで、温かく明るく。
- ✅ 影と枠は暖色。余白を活かして「明るさ」を作る。
- ✅ レイアウトと情報設計は維持(診断の機能を壊さない)。
- ❌ 純白(#ffffff)・純黒(#000)・冷たい青グレーを地や文字に使わない。
- ❌ 原色のビビッドなアクセントを多色で散らさない(桜ピンク主役・オレンジ副に絞る)。
- ❌ トークンを使わずページ内に色を直書きしない(このファイルが色の正本)。
