/**
 * 拡張の更新履歴データと semver 比較ヘルパ。
 * 直近20バージョンのみ同梱（旧版は changelog-archive.js）。
 *
 * 設計（0.1.12 D: 更新履歴 popup 表示）:
 *   ・version 文字列・日付・概要・項目配列を JSON-like なデータ構造で保持。
 *   ・popup-entry.js が <details id="changelogPanel"> の中身として描画する。
 *   ・各項目は HTML を含まずプレーンテキスト。
 *
 * @typedef {{
 *   version: string,
 *   date: string,
 *   summary: string,
 *   items: readonly string[]
 * }} ChangelogEntry
 */

/** @type {readonly ChangelogEntry[]} */
export const EXTENSION_CHANGELOG = Object.freeze([
  Object.freeze({
    version: '0.1.1516',
    date: '2026-09-15',
    summary: 'ランキングのホバー発言を先読みで速く',
    items: Object.freeze([
      '追憶のきらめき ランキング(/live/)で、コメント順位表の行にマウスを乗せたとき出る「その人の発言」を、ページを開いた時点で上位の配信ぶん先読みするようにしました。初回の待ち時間が減ります。',
      '取得中は骨格(スケルトン)を表示して、待っている間の見た目をなめらかにしました。'
    ])
  }),
  Object.freeze({
    version: '0.1.1515',
    date: '2026-09-15',
    summary: '応援レーンのタイルから発言一覧を開けるように',
    items: Object.freeze([
      '応援レーンのアイコンを押すと、その人がこの配信でした発言を新しい順に読めます(Ctrl+クリックで従来どおりプロフィール)。会場モードの発言パネルも 1,000 件まで表示します。'
    ])
  }),
  Object.freeze({
    version: '0.1.1514',
    date: '2026-09-15',
    summary: 'ランキングの名前ホバーで直近の発言を表示',
    items: Object.freeze([
      '追憶のきらめき ランキング(/live/)で「コメントで応援した人」の名前にマウスを乗せると、その人の直近の発言を最大 5 件その場で取得して表示します。本文はサーバーに保存しません。',
      '各行をサムネ・名前・件数まとめて 1 つのリンクにしました(数値 ID の人はユーザーページへ)。'
    ])
  }),
  Object.freeze({
    version: '0.1.1513',
    date: '2026-09-15',
    summary: 'コメント集計を増分にし匿名の重複名を解消',
    items: Object.freeze([
      '追憶のきらめき ランキング(/live/)のコメント集計が、前回数えた続きだけを読むようになりました。長い配信でも「直近ぶん」で止まりにくくなり、古い側も順次さかのぼります。',
      '同じ順位表に同じ『匿名NNN』が 2 人出たときだけ、区別のための短い記号を添えます。'
    ])
  }),
  Object.freeze({
    version: '0.1.1512',
    date: '2026-09-14',
    summary: 'ランキングの3列目が狭い画面ではみ出す不具合を修正',
    items: Object.freeze([
      '追憶のきらめき ランキング(/live/)で、画面幅が 720〜900px のとき「コメントで応援した人」の列が右にはみ出していたのを直しました。3 列が同じ幅で収まり、長い名前は「…」で省略されます。'
    ])
  }),
  Object.freeze({
    version: '0.1.1511',
    date: '2026-09-14',
    summary: 'コメントで応援した人の枠を足しました',
    items: Object.freeze([
      '★追憶のきらめき ランキング(/live/)に 3 つめの枠「コメントで応援した人」を足しました。ギフト・広告に並べて、その配信でコメントした人を件数の多い順に出します。',
      'この件数だけは当サイトが数えた値です(ギフト・広告はニコニコ生放送の公開値そのまま)。誰が何件かだけを数え、コメントの本文・時刻は保存していません。',
      '匿名(184)の方も件数順にそのまま並びます。その番組の中だけで通じる識別子から「匿名＋番号」と似顔絵を作って表示します(アカウントや個人は特定できません)。'
    ])
  }),
  Object.freeze({
    version: '0.1.1510',
    date: '2026-09-14',
    summary: 'ランキングを X でシェアできるようにしました',
    items: Object.freeze([
      '★追憶のきらめき ランキング(/live/)の各配信に「X でシェア」を付けました。押すと X の投稿画面が開き、配信者名・番組名・その配信へのリンク・#ニコ生 が下書きに入ります(投稿するかどうかはご自身で決めます)。',
      'シェアされたリンク(?lv=付き)を開くと、その配信が一覧の先頭に出ます。終了している場合はその旨を添えて、いま支えている人の一覧をそのまま表示します。',
      'X などに貼ったときのカード画像と説明文を、ランキング専用のものにしました。当サイトはシェアを押した事実を含め何も記録しません(プライバシーポリシー §14-2 に明記)。'
    ])
  }),
  Object.freeze({
    version: '0.1.1509',
    date: '2026-09-14',
    summary: 'サイドパネルが固まる・黒いままの真因を直しました',
    items: Object.freeze([
      '★コメントが増えるとサイドパネルが数十秒〜数分止まり、Chromeの「読み込み中」(黒地に横縞)が出続けてスクロールもできなくなっていました。実際の処理時間を測って真因を特定しました。',
      '★原因は、応援者のアイコンを引くたびに記録コメント全件を頭から見直していたことです(コメント2万件で4.7億回の比較)。1回だけ索引を作って引く形にし、答えは前と同じままです。',
      '★状態速報の「最大タイマー遅延」が 50秒→178秒 と悪化していた値が、この版で下がるかを次の実機で確認します。'
    ])
  }),
  Object.freeze({
    version: '0.1.1508',
    date: '2026-09-14',
    summary: '出荷前の自己点検を横断キットと共通化',
    items: Object.freeze([
      '★拡張を出す前の自己点検を、他のアプリと共通の診断キットで行うようにしました。依存関係の食い違い・秘密情報の混入・巨大ファイル・似た実装の増殖・検査の配線漏れなどを、出荷のたびに機械で見ます。',
      '★「サムネが白い」「アイコンが全部読めない」「レーンが空」という症状名から、原因の候補と次の一手を横断の索引で引けるようにしました。同じ症状で調べ直す往復が1回減ります。',
      '★AI向けの指示書が「存在しない検査」を指していた箇所を直し、その検査を実際に作りました。指示書の入口(1行目の読み込み)が壊れていないかを機械で守ります。'
    ])
  }),
  Object.freeze({
    version: '0.1.1507',
    date: '2026-09-04',
    summary: '埋め込みに別の配信が映る問題を直しました',
    items: Object.freeze([
      '★配信ページに埋め込まれた画面に、別の配信の応援レーンやランキングがそのまま残ってしまうことがありました。今どの配信を見ているかを照合してから表示するようにしました。',
      '★同じ照合は本体のポップアップ側には元からありましたが、埋め込み側の5つの表示(応援レーン・コメント・貢献度/広告ランキング・数字カード・応援者ランキング)には入っていませんでした。',
      '★どちらの配信か分からないときは、あえて表示を止めません。何も映らないほうが原因が分かりにくいためです。'
    ])
  }),
  Object.freeze({
    version: '0.1.1506',
    date: '2026-09-04',
    summary: '設定の保存先の名前を1箇所にまとめました',
    items: Object.freeze([
      '★読み上げの設定と「最後に見た配信」の保存先の名前が、複数の場所に同じものが書かれていました。片方だけ直して食い違う事故を防ぐため、1箇所にまとめました。',
      '★同じ名前が2箇所以上に増えたら自動で気づける検査も入れました。人の注意力に頼らない形にしています。',
      '★出荷物に秘密の情報が混ざっていないか調べる検査に、「わざと壊して本当に見つけられるか」を自分で確かめる仕組みを付けました。'
    ])
  }),
  Object.freeze({
    version: '0.1.1505',
    date: '2026-09-03',
    summary: '提出物づくりで版数を間違えられなくした',
    items: Object.freeze([
      '★ストア提出物を作るスクリプトが、版数の入力ミスをそのまま素通しして作業を進めてしまう不具合を直しました。',
      '正しい形の版数、かつ現在の版数と一致しているときだけ提出物を作るようにしました。'
    ])
  }),
  Object.freeze({
    version: '0.1.1504',
    date: '2026-09-03',
    summary: '診断ページの説明文の誤りを訂正',
    items: Object.freeze([
      '★診断ページのソースコード内の説明文に、事実と異なる記載がありました。実際の動きは変わりません。'
    ])
  }),
  Object.freeze({
    version: '0.1.1503',
    date: '2026-09-03',
    summary: '無関係な更新で描き直す回数を減らしました',
    items: Object.freeze([
      '★画面と関係ない裏側の更新のせいで、無駄に描き直っていた回数を減らしました。体感の重さが少し軽くなるはずです。',
      '★診断用のデータの更新や、前に見た配信情報の保存が、余計な描き直しを引き起こしていました。',
      'これらは表示の正しさには関わらない裏方の処理だったため、描き直しの引き金から外しました。'
    ])
  }),
  Object.freeze({
    version: '0.1.1502',
    date: '2026-09-03',
    summary: 'コメビュの窓の大きさを覚えます',
    items: Object.freeze([
      '★コメビュの別窓とOBS窓が、前に使った大きさと位置で開くようになりました。これまでは毎回きまった大きさに戻っていました。',
      '★OBSに重ねて使っている場合、開き直すたびに配信画面の合わせ直しが必要でしたが、それが要らなくなります。',
      '最小化したまま閉じたときは覚えません(次に潰れた窓が出ないようにするためです)。',
      '画面の外に出てしまうような位置も覚えません(見えない窓ができないようにするためです)。',
      '★あわせて、描き直しの理由を記録する計器の内訳がずれていたのを直しました(前の理由が残って別の理由として数えられていました)。'
    ])
  }),
  Object.freeze({
    version: '0.1.1501',
    date: '2026-09-03',
    summary: 'コメビュのちらつきを直しました',
    items: Object.freeze([
      '★コメントビューアが1分おきに一瞬ちらついていたのを直しました。アイコンが消えて出直す形で見えていた症状です。',
      '★原因は、1分ごとに画面の中身を全部作り直していたことでした。作り直すとアイコンの画像も取り直しになるため、その瞬間だけ空白になっていました。',
      '★これからは「前と変わっていない行はそのまま残す」ようにしました。変化が無いときは画面に一切触りません。',
      '並び替えなど、差分では表せない変化のときだけ、今までどおり作り直します(取りこぼしを防ぐため)。'
    ])
  }),
  Object.freeze({
    version: '0.1.1500',
    date: '2026-09-02',
    summary: '診断ページで会場も見られます',
    items: Object.freeze([
      '★診断ページに、ポップアップに続いて【会場モード】も埋め込みました。詰めている数字と、実際の画面を、同じ場所で見比べられます。',
      '★以前は「会場は奇抜な描き方をしているので埋め込むと重い」と判断していましたが、その描き方は既に別の版でやめていました。古い前提だけが残っていたので、確かめてから入れました。',
      '★会場の画面は会場自身が作るので、診断ページの部品数は増えません。読み取りも増やしていません。',
      '★重かった場合は1行で消せるようにしてあります。'
    ])
  }),
  Object.freeze({
    version: '0.1.1499',
    date: '2026-08-31',
    summary: '診断ページでポップアップを見られます',
    items: Object.freeze([
      '★診断ページに「ポップアップそっくり」の埋め込みを戻しました。診断を見ながら、同じ画面で応援レーンや数字を確かめられます。画面を行き来する手間が減ります。',
      '★この機能は以前「勝手に別の配信タブが開く」症状の【疑い】で止めていましたが、その後の調査で真因は別の機能（自動巡回）だと確定しており、そちらは既に止めてあります。巻き添えで止まったままだったのを戻しました。',
      '★埋め込みのポップアップは【見るだけ】のモードで開きます（保存しない・放送ページに入り込まない・取得しない）。純Web版でも同じモードを使っています。',
      '★万が一のときは1行で元に戻せるようにしてあります。戻すべき場所と、過去の経緯もコードに残しました。'
    ])
  }),
  Object.freeze({
    version: '0.1.1498',
    date: '2026-08-31',
    summary: '間違った警告を出していたのをやめました',
    items: Object.freeze([
      '★状態速報で「部品数が推奈1,500を超過」と警告を出していましたが、この基準は古くなっていました。実際に測ったところ、健全な状態でも警告が出ていました。紛らわしくて申し訳ありません。',
      '★基準の元であるChromeの検査項目が 2025年10月に廃止され、「部品の数」ではなく「実際に描画が何秒止まったか」で見る形に変わっていました。',
      '★実測したところ、部品が7,000個あっても描画は15.6ミリ秒でした（問題とされるのは40ミリ秒から）。つまり実害は出ていませんでした。',
      '★数字自体は引き続き表示します。桁が違うときは実際に固まった実績があるためです。ただし「多すぎる」との判定は、止まった時間を測れるようになるまでしません。'
    ])
  }),
  Object.freeze({
    version: '0.1.1497',
    date: '2026-08-30',
    summary: 'できない手順を案内していたのを直しました',
    items: Object.freeze([
      '★状態速報の対処案内で、WEB共有のキーが未設定のときに「.env に設定して再ビルドしてください」と出ていました。この方法は以前に廃止されており、そのとおりにしても直りませんでした。申し訳ありません。',
      '★同じ速報の別の行は正しく「「WEB共有の設定」で入力してください」と書いており、1つの速報が矛盾した案内を2つ出していました。正しい方に揃えました。',
      '★廃止された手順を再び案内しないよう、検査を足しました。'
    ])
  })
]);


/**
 * 先頭（最新）の changelog エントリを返す。
 * @returns {ChangelogEntry}
 */
export function getLatestChangelogEntry() {
  return EXTENSION_CHANGELOG[0];
}

/**
 * `MAJOR.MINOR.PATCH` の semver を数値として比較する。
 *   compareSemver('0.1.10', '0.1.9') > 0  // 文字列比較だと逆になるので注意
 * @param {string} a
 * @param {string} b
 * @returns {number} a > b で正、a < b で負、同値で 0
 */
export function compareSemver(a, b) {
  const pa = String(a || '0.0.0').split('.').map((n) => Number(n) || 0);
  const pb = String(b || '0.0.0').split('.').map((n) => Number(n) || 0);
  const len = Math.max(pa.length, pb.length, 3);
  for (let i = 0; i < len; i++) {
    const va = pa[i] ?? 0;
    const vb = pb[i] ?? 0;
    if (va !== vb) return va - vb;
  }
  return 0;
}
