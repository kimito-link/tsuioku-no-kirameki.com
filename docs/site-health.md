# サイト健全性チェック（自動生成）

> `scripts/site-health.mjs` が git 追跡の公開ページ(LP/記事/docs/council)を静的解析。**手で編集しない**。
> 検証: **相対内部リンク(.html/.md)の参照先がディスクに実在するか**。外部リンクは叩かない(依存/プライバシー/速度)。
> 再生成: `npm run site-health` ／ 検証のみ: `npm run site-health:check`(リンク切れで exit 1)。

検証対象: 95 ファイル

## ✅ 内部リンク健全性: 問題なし

相対内部リンクの参照先はすべて実在します。
