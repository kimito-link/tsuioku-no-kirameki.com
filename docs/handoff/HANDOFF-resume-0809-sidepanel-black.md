# 引き継ぎ: 2026-08-09（v0.1.1292〜1296）サイドパネル黒画面・未解決

> **この1枚で再開できる。まず §0 と §1 を読む。**
> 前段の計画は `mirror-writer-decoupling-PLAN-2026-08-08.md`（★§2 の結論で【不要】になった）。

---

## 0. 現在地（30秒）

```
ブランチ : feat/mirror-writer-decoupling
HEAD     : 5b1199cf  v0.1.1296
作業ツリー: クリーン（push 済み）
verify:cc: 全10ステップ OK / 単体 9,000件超 緑
```

**★最優先の未解決 = サイドパネルが黒いまま（ユーザー実機のみ・6回直して未解決）**

---

## 1. ★次にやること（1つだけ）

**ユーザーに「黒い状態で状態速報をコピー」してもらい、この行を読む:**

```
サイドパネル自己診断: 🔴黒くなりうる / v0.1.xxxx / 幅x高 / 外✅ iframe🔴 中✅ / 原因=iframeが潰れている(0x0)
```

v0.1.1296 でこの行が**実機の状態速報に出ることを実測確認済み**（§4 参照）。
原因が7候補から1つに名指しされるので、**推測せずに直せる**。

| 名指しされる原因 | 次に見る場所 |
|---|---|
| 外側(sidepanel.html)が塗っていない | `extension/sidepanel.html` の `<html style=...>` |
| **iframeが潰れている(WxH)** | サイドパネル幅とレイアウト（有力候補） |
| iframeの中身を読めない | ロード失敗/別オリジン |
| **中身が空(bodyの子0)** | popup.html の描画失敗 |
| **幕(cloak)が残っている** | JSが途中で止まっている |
| 中身(popup.html)が塗っていない | popup.html の背景指定 |
| color-schemeがlightでない | ダーク指定の残り |

★`v` も出る。**v0.1.1295 以前なら「修正が届いていないだけ」**と即判定できる。

---

## 2. ★大改修は【不要】と確定した（段1の成果）

`mirror-writer-decoupling-PLAN` の段2〜6（鏡の書き手を content へ移す）は**丸ごと不要**。
GPTと3往復して設計したが、計器を入れて測ったら前提が崩れた。

実機の計器（v0.1.1292 で追加）:
```
lanePublishSkip: noEls=0 / entriesEmpty=0 / lastPublishAgoSec=0
```
= **publish は完璧に動いている**。3つの仮説すべて外れ。

★1枚目の速報で鏡が🔴に見えたのは**起動1.4秒後に撮った早すぎる値**。
　2枚目では全部 `✅あり`・パリティも `✅一致` だった。

---

## 3. このセッションで直したもの

| 版 | 内容 | 真因 |
|---|---|---|
| v1292 | 鏡publishの到達/見送り計器 | （観測のみ）→ §2 の結論を出した |
| v1293 | **応援ビジュアルが1クリックで開かない** | `content-visibility:auto` が**当たり判定を消す** |
| v1294 | サイドパネル黒の「残り」 | 外側(sidepanel.html)の `<html>` にインライン style が無かった |
| v1295 | サイドパネル自己診断 | （新機能） |
| v1296 | **自己診断が状態速報に出ていなかった** | 受け渡しが2か所途切れ（undefined に落ちる） |

### v1293 の詳細（実害あり・e2eが08-05から赤だった真因）
`content-visibility: auto` はスキップ中の subtree を**ヒットテストから外す**。
同じ点・同じ瞬間で実測:
```
.nl-stats                  auto → elementFromPoint=SECTION / visible → DETAILS ✅
.nl-support-visual-details auto → elementFromPoint=DETAILS / visible → SUMMARY ✅
```
★「`<summary>` は直接の子だから当たり判定は残る」は**誤り**（subtree全体がスキップ）。
検査 = `src/lib/contentVisibilityHitTest.wiring.test.js`

---

## 4. ★私がやらかしたこと（同じ轍を踏まないため）

### (a) 裏取りせずに出荷した（v1295）
「これで原因が分かります」と言いながら、**その行が実際に出るかを一度も確かめなかった**。
ユーザーの速報に行が無く発覚。ユーザーの指摘「裏どりしてないですよね？」が的確だった。

→ v1296 で通し検査（`sidepanelSelfDiagPipeline.test.js`）を追加。
　**storage → pick → renderAll受け取り → 整形呼び出し → 実際の出力文字列** の5段を断言。
　★この検査が**2つ目の断線を発見した**（1つ目を直した時点で私は「直った」と思っていた）。

### (b) 自分の書いたテストが2回【恒真】だった
- `[\s\S]{0,400}?` の緩いギャップ → `if(false)` 前置を**素通し**
- `indexOf + 1400文字` の緩い切り出し → 壊す変異を**素通し**

→ 対策: **行頭アンカー `^\s*...$` + `m`** / **`{...}` を厳密に切り出す**。
　書いた直後に必ず壊して赤を確認する。

### (c) 誤読を2回した
- `deferHeavy=28/35` を「応援レーンが描画を止めている」と誤読
  → 実際は**北極星の独立tick**を数えるもの。応援レーンは `settled`・69件描画で正常だった
- スクショに**会場ステージが写っていない**のに、会場遮蔽の経路を調べていた

→ 数字を見たら**何を数えているか**を先に確認する。

### (d) 測定環境の取り違え
devtools MCP から拡張が外れているのに気づかず、**何も入っていない状態を測って**
「塗られていない」と結論しかけた。→ `list_extensions` で毎回確認する。

---

## 5. 環境の制約（重要）

- **MCP は私専用の隔離ブラウザに繋がる**。ユーザーの Chrome（ニコ生タブ）には**届かない**
- `claude-in-chrome` は未接続（`[]`）
- → **実機の値はユーザーに取ってもらうしかない**。だから自己診断を作った

---

## 6. 残っている未解決

| 件 | 状態 |
|---|---|
| **サイドパネル黒** | ★最優先。§1 の1行待ち |
| 会場モードが①と同じにならない | 未着手。①が描画を見送る設計との兼ね合いあり |
| watchページの崩れ（レーンが動画に重なる） | 未確定。スクショ1枚目のみ・再現条件不明 |
| `timeline-fill-standalone-window` e2e | 調査済み・**直せていない**。詳細は `HANDOFF-resume-0808-e2e-content-visibility.md` §4 |
| 記録109%の二重計上 | 計器が「過剰増」を名指し中 |
| e2e 残り6件 | master も08-05から赤=このブランチ由来ではない |

---

## 7. テストについての結論（ユーザーの質問への回答）

このプロジェクトは**単体679ファイル・9,000件超 / e2e 69ファイル**。
「テストを書く」段階は達成済み。次は **「テストが嘘をつかないか確かめる」** 段階。

今日6件のバグのうち、テストが効いたのは3件、**効かなかった3件は全部「緑だったのに動いていない」**。

効くのはこの3つ:
1. 書いた直後に**壊して赤を確認**する（恒真なテストは無いより悪い）
2. **「部品が動く」と「端から端まで届く」を分ける**（v1296 がまさにこれ）
3. **症状の時間スケールに測り方を合わせる**（「一瞬」は settled state では見えない）
