# `/live/` 支えた人のリアルタイムランキング — 計画

> ★この文書はリポ内の正本。元は Claude Code のプラン領域にあり開けなかったのでここへ移した。
> ★Step 0（成立確認）は **2026-09-05 に実測完了**。結果は末尾「Step 0 実測結果」に追記。

# 続き6（2026-09-05）★`tsuioku-no-kirameki.com/live/` — 支えた人のリアルタイムランキング

## Context（なぜ作るか）

ユーザーの言葉（原文・時系列）:
> 追憶のきらめきランキングみたいなのつくりたい
> **まったくおなじじゃつまらない**
> このアプリは**リアルタイム性** / リアルタイムに**うごきがほしい**
> 今週とかじゃなくて / **今の瞬間**
> `https://www.chikuwachan.com/live/` みたいに **`https://tsuioku-no-kirameki.com/live/`** としたい

★参照した競合2つ:
| | 出しているもの | 更新 |
|---|---|---|
| ニコ生AI新聞 | 配信者の**フォロワー増加数** | ★毎朝5時・1日1回 |
| chikuwachan `/live/` | ★**「リアルタイムランキング」を名乗る**・複数プラットフォーム横断の配信者一覧 | 🤖自動更新（頻度は明示なし） |

⟹ ★**「配信者を並べる」で正面から行くと勝てない**（chikuwachan が既にリアルタイムを名乗っている）。

---

## ★差別化の一点（これが企画の芯）

★**主役を「配信者」ではなく「支えた人」にする。**

`kokenGiftHistoryApi.js:22-25`（実機確認済みの正本スキーマ・実物）:
```
histories:[ { id, supporterId, supporterName, supporterThumbnailUrl,
              itemName, itemThumbnailUrl, point, contribution, publishedAt } ]
★ `supporterId` は**数値 uid**（DOM scrape では取れなかった）
```
⟹ ★**誰がいくら投げたかが取れる。** chikuwachan も新聞も出していない軸。

★追憶の思想そのもの（`AGENTS.md`・LP・`backfillRinkuNarration.js` に一貫）＝
**「誰が応援してくれたか」**。★先行事例なしと調査済み（[[gift-viewer-market-gap-2026-08-12]]）。

---

## ★実測で確定した事実

### 事実1: ★URL は配線済み（ページを置くだけで通る）
`vercel.json:20-23`（実物）:
```json
{ "source": "/:path((?!tsuioku-no-kirameki/|app/|extension/|api/|_next/).*)",
  "missing": [{ "type": "host", "value": "app.tsuioku-no-kirameki.com" }],
  "destination": "/tsuioku-no-kirameki/:path" }
```
⟹ ★`tsuioku-no-kirameki.com/live/` → `/tsuioku-no-kirameki/live/` へ既に流れる。**ルーティング変更は不要**。
★現状 `tsuioku-no-kirameki/` に `live` は**0件**（新規）。

### 事実2: ★公開Web版とサーバは既にある（作り直さない）
| 資産 | 実体 |
|---|---|
| 純Web版 | `app/live-view.html` + `app/live-view.js`（★本物の `popup-entry.js` を import して同じ画面を出す） |
| サーバ | `api/status.js`（Vercel Function + Upstash Redis・npm依存ゼロ） |
| 描画の正本 | `paintTopSupportRankStyleIntoElement.js:101` `renderTopSupportRankStripInto`（popup と純Web で**共有済み**） |

### 事実3: ★★現行サーバは横断を持てない（ここが本丸）
`api/status.js:12`（実物）: 「キーは `status:latest:<v>`」／TTL 7日
- ★**1トークン=1配信の最新スナップショットを上書き**するだけ
- ★配信者IDも liveId もキーに入らない／**全トークンの一覧を取る手段が無い**
- ★収集は**受動**（拡張ユーザーが送ってきた分だけ）。能動クロールは存在しない

### 事実4: ★材料APIは認証不要（第三者が任意の配信を取れる）
| API | 実測記述（コメント原文） |
|---|---|
| `kokenGiftHistoryApi.js:12` | 「認証不要（cookie / X-Frontend-Id 全省略・`credentials:'omit'` でも 200）」 |
| `nicoadContributionRankingApi.js:14` | 「認証不要（cookie / ヘッダ省略でも 200）」 |
| `kokenContributionRankingApi.js:16` | 「認証不要（… 全ヘッダ省略でも 200）」 |
★いずれも「2026-05-19〜06-01の実機ネットワーク観測で確証」と明記。
★拡張では CORS のため SW 特権 fetch が要るが、★**サーバ間通信に CORS は効かない**
　⟹ Vercel Function から直接叩ける（★これは論理的帰結。未実測）。

### 事実5: ★送信の同意設計は既に正しい（壊さない）
`autoPublishDecision.js:62`（実物）: `if (a.optedIn !== true) return { publish: false, reason: 'no_consent' };`
★既定 `false`・読めなければ `false`（fail-closed）。`privacy.html:542-583` と**一致**（調査で確認）。
★過去の事故（v0.1.1242・同意ゲート皆無で120秒ごとに第三者のコメントを送信していた）は修正済み。

### ★事実6: 未解決 — 「今放送中の番組一覧」を取る手段が【リポに無い】
```
grep: onair / on_air / 放送中一覧 / getRecent → ★0件
```
★横断ランキングは「どの lv を見るか」が決まらないと**始まらない**。★ここが最大の未知数。

---

## ★やること（★調査 → 最小実装 の順。飛ばさない）

### ★Step 0 — 成立するかを確かめる（★実装ゼロ・これが無いと全部が絵に描いた餅）
| 確かめること | 方法 | 落ちたら |
|---|---|---|
| ★①「今放送中の lv 一覧」を認証不要で取れるか | ニコ生の公開エンドポイントを1本ずつ `curl` して実測 | ★取れなければ**横断は諦め**、Step 3（自分の配信だけ）へ縮退 |
| ★②サーバから koken/nicoad を叩けるか | Vercel Function から1本だけ実際に fetch して 200 を見る | ★403等なら別経路（拡張が送る受動収集）へ |
| ★③レート制限 | 連続 N 本でどこから落ちるか | 取得間隔と対象数の上限を決める |

★**Step 0 の結果を計画に書き戻してから Step 1 へ**。★「取れるはず」で先に作らない。

### ★Step 1 — `/live/` を1枚出す（★中身は自分のデータ・横断はまだ）
| | |
|---|---|
| 新規 | `tsuioku-no-kirameki/live/index.html` |
| 中身 | ★**支えた人ランキング**（ギフト＋広告＋コメント）。★`renderTopSupportRankStripInto` を再利用（似せて自作しない） |
| データ | ★まず `api/status.js` の既存トークン経路（＝自分が公開した1配信） |

★**なぜ横断から始めないか**: ★Step 0 が落ちる可能性があり、
　落ちても `/live/` というURLと見た目は残る＝**捨てにならない**。

### ★Step 2 — リアルタイムに「動かす」
★ユーザーの要望の核心。★ただし**土台が詰まっているので順序に条件を付ける**（下記「先に直すもの」）。
- 順位が入れ替わったら行が**移動して見える**（FLIP）／応援が届いた瞬間その行が光る
- ★`comeviewTimelineDiff.js`(v0.1.1501) の「変化が無ければDOMを触らない」を土台にする
- ★**要素を作り直さない**（作り直すと必ずちらつく＝v0.1.671/672 二重表示・今日ずっと追った問題）

### ★Step 3 — 横断（★Step 0 が通ったときだけ）
- サーバ側に「対象 lv リスト → 各APIを叩く → 集計 → 保存」を足す
- ★保存構造を変える必要がある（現状 `status:latest:<token>` は横断不可）
- ★**送信の同意設計には触らない**（事実5・`optedIn` ゲートはそのまま）

---

## ★先に直すもの（★Step 2 の前提・スクショの実測）

ユーザー提供の状態速報（2026-09-05 5:49・実機）:
```
🔴可視中でも配達が1秒超=iframeのイベントループが詰まっている(描画側を直しても消えない)
表示遅延 直近253142ms(平均127048ms)      ← ★253秒
描き直しが多い(コメント1件につき3.5回)   ← 正常は3回以下
最多の理由は unknown(61%)                ← ★今日の v0.1.1502 が効いていない証拠
```
★別配信では `表示遅延 直近22ms(平均49ms) / ✅可視中の配達は健全`。**同じ拡張で桁が違う**。

★`eventLoopStallSummary.js:19-24` が判定を明記（実物）:
> 遅延が大きい(凍結) … ★**描画側をいくら直しても無駄**。スレッドを止めている物を探す

⟹ ★**253秒のまま順位アニメを乗せると「2分前の順位が、なめらかに動く」**。
　　★リアルタイムを名乗れない。★Step 2 の前に、可視中の配達が1秒を切ることを実測で確認する。

★調査の入口2つ（★どちらも今日の作業の続き）:
1. ★`unknown(61%)` の正体 — `_refreshReasonTag` を通らない再描画経路が残っている
2. ★配信によって桁が違う理由 — 片方だけ詰まる条件（コメント量？ギフト量？裏タブ滞在？）

---

## ★やらないこと（蒸し返し防止）

| やらないこと | 理由 |
|---|---|
| ★配信者ランキングで正面勝負 | ★chikuwachan が既に「リアルタイムランキング」を名乗り**複数プラットフォーム横断**。勝てない。★主役を「支えた人」にずらす |
| ★フォロワー増加数を出す | ★ニコ生AI新聞と同じもの。公開APIを叩けば誰でも作れる＝**真似され返される** |
| ★`renderTopSupportRankStripInto` に似せた新実装 | ★popup と純Web が**共有している正本**。似せて自作しない（このリポの掟） |
| ★`vercel.json` を触る | ★`/live/` は**既に配線済み**。触ると他のページを壊す |
| ★同意ゲート(`optedIn`)を緩める | ★v0.1.1242 の実事故（CWS アイテム停止級）。★`privacy.html` と一致している状態を壊さない |
| ★253秒を放置して Step 2 に進む | ★計器自身が「描画側を直しても無駄」と判定している |
| ★「取れるはず」で Step 3 を先に作る | ★事実6（放送中一覧の手段が無い）が未解決。★Step 0 で落ちたら全部無駄になる |

---

## リスクと戻し方

| リスク | 兆候 | 戻し方 |
|---|---|---|
| ★Step 0 で放送中一覧が取れない | curl が 403/404 | ★横断を捨て Step 1+2（自分の配信）で完成させる。★`/live/` は残る |
| ★API のレート制限に当たる | 連続取得で 429 | 取得間隔を延ばす／対象を上位N件に絞る |
| ★順位アニメでちらつく | アバターが一瞬消える・二重表示 | ★Step 2 を revert（Step 1 の静的表示に戻る） |
| ★第三者データの公開範囲 | — | ★`supporterName` は**ニコ生が公開している値**だが、★集約して常時公開する形は別問題。★Step 3 の前に `privacy.html` を同一コミットで更新（[[disclosure-must-match-behavior-2026-08-03]]） |

---

## 検証

```bash
npm run verify:cc
```

★**Step 0（最重要・実装前）**: エンドポイントを実際に叩いた**生の応答**を計画に貼る。
　★「取れるはず」を根拠にしない。

★**Step 1**: `/live/` をブラウザで開いて、支えた人が出ること。★`vercel.json` 無変更で 200 が返ること。

★**Step 2 の毒テスト**:
| 毒 | 期待 |
|---|---|
| 同じ順位のデータを2回渡す | ★DOMを触らない（＝要素の同一性が保たれる）テストが赤にならない |
| 要素を作り直す実装に戻す | ★「同一キーの要素が再利用される」テストが赤 |

★**Step 2 の実機**: ★可視中の配達平均が**1秒未満**であることを状態速報で確認してから着手する。
　★253秒のままアニメを入れて「動いた」と言わない。

---

# ★Step 0 実測結果（2026-09-05・★全部通った）

> ★計画に「取れるはずで作らない／生の応答を貼る」と書いたので、実際に叩いた結果をそのまま残す。

## ★① 放送中の lv 一覧 — 取れた（最大の未知数が解けた）

叩いたもの（4本試して1本当たり）:
```
https://live.nicovideo.jp/api/relive/notifybox.content      -> 404
https://live.nicovideo.jp/front/api/v1/recommend-contents   -> 503
https://live.nicovideo.jp/front/api/v2/recommend-contents   -> 400（★存在はする＝パラメータ不足）
https://api.search.nicovideo.jp/api/v2/live/contents/search -> 403
★https://live.nicovideo.jp/ranking                          -> 200
```
★`/ranking` のHTMLから **lv が 65件** 取れた（認証・ヘッダ一切なし）:
```
lv351173459 lv351185201 lv351185202 lv351236047 lv351264029 ...（65件）
```
★埋め込みJSONは無し（`"liveId"`/`"isOnAir"` 等は0件）＝**HTMLから lv を拾う形**になる。
★★未確認: この65件が「放送中」だけかは未検証（ランキングなので終了番組が混ざる可能性）。
　★Step 3 の前に1件ずつ生存確認する経路が要る。

## ★② 材料API 3本 — 全部 200・認証不要（コメントの記述どおりだった）

`lv351173459` で実測:
```
koken ギフト履歴   -> 200
koken 貢献度ランキング -> 200
nicoad 広告ランキング  -> 200
```

★生の応答（**支えた人が名前・ID・金額つきで、既に順位づけ済み**で返る）:
```json
// koken histories
{"data":{"totalPoint":158270,"histories":[
  {"supporterName":"名無し","itemName":"シュシュ20260904","point":5000,"contribution":500}]}}

// koken ranking
{"data":{"rankers":[
  {"rank":1,"supporterName":"名無し","contribution":100000},
  {"rank":2,"supporterId":2566426,"supporterName":"Raysword",...}]}}

// nicoad ranking
{"data":{"contentTotalContribution":1269449,"ranking":[
  {"userId":19190613,"advertiserName":"かかし","totalContribution":52794,"rank":1,
   "userPageUrl":"https://www.nicovideo.jp/user/19190613"}]}}
```
⟹ ★**順位を自分で計算する必要すら無い。** ニコ生側が rank を持っている。
⟹ ★`supporterId` / `userId` / `userPageUrl` まで返る＝人物ページへリンクできる。

## ★③ レート制限 — 10本連続・間隔ゼロで全部200

```
lv351173459:200 lv351185201:200 lv351185202:200 lv351236047:200 lv351264029:200
lv351274111:200 lv351274727:200 lv351274728:200 lv351274729:200 lv351274730:200
```
★★**ただし「10本での結果」**。65本一括や、数十秒ごとの継続ポーリングは**未検証**。
　★Step 3 で常時運用する前に、実運用の間隔で1回測ること。

---

## ★Step 0 の結論

| 項目 | 判定 |
|---|---|
| 放送中 lv 一覧 | ★取れる（`/ranking` のHTML・65件） |
| 支えた人のデータ | ★取れる（3本とも200・名前/ID/金額/rank つき） |
| サーバから叩けるか | ★**curl で叩けた＝サーバ間通信で問題ない**（CORSは効かない・論理的帰結を実測で裏付け） |
| レート制限 | ★10本では当たらない（★継続運用は未検証） |

⟹ ★**横断ランキングは成立する。** Step 1 へ進んでよい。
★ただし「放送中だけか」「継続ポーリングの可否」の2点は**未確認のまま残す**。

## ★Step 0 で分かった設計上の収穫

★当初「ギフト＋広告＋コメントを自分で合算する」つもりだったが、**その必要が無い**:
- koken が `contribution` で順位済み
- nicoad が `totalContribution` で順位済み
⟹ ★**2つの公式ランキングを1画面に並べるだけ**でも「支えた人」が出せる。
　★実装が一段軽くなる。自前集計は後から足せばよい。
