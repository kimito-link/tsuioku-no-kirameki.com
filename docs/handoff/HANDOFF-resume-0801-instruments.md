# セッション引き継ぎ 2026-08-01 後半（v0.1.1209 → v0.1.1215）

> このファイルを最初に読めば状況を復元できます。
> 同日前半の引き継ぎ: `docs/handoff/HANDOFF-resume-0801.md`（v0.1.1194→1208）

## 現在地（機械的事実）

| 項目 | 値 |
|---|---|
| ブランチ | `master`（全て push 済み） |
| リポジトリ版 | **v0.1.1215** |
| 配布済み拡張（`C:\nicolive-ext`） | **v0.1.1215** |
| 本番LP | v0.1.1215（`verify:bump [6]` で機械照合） |
| `npm run verify:cc` | **全ステップ緑** |
| 未マージのブランチ | なし |

---

## このセッションの性格：ほぼ全部「計器の穴を塞ぐ」作業だった

7版進めたが、実質は**私（Claude）が実配信の速報から5回誤判定しかけ、その原因が
すべて「計器が症状しか出さず原因を出さない」ことだった**という話に尽きる。

| # | 私が疑ったもの | 実際 | 対処 |
|---|---|---|---|
| 1 | 鏡publishの取りこぼし | 起動0.4秒の値を見ていただけ | v0.1.1211 |
| 2 | 記録の二重計上が再発 | popup起動時の初期読み込み | （速報が🟢に変わり自己解決） |
| 3 | 会場のCSS不整合 | CSSは同一。測定対象がズレていた | v0.1.1212 |
| 4 | ゆっくり顔の桁境界バグ | 匿名広告主は構造的に必ずそうなる仕様 | 調査のみ |
| 5 | グリッドは「積み上がりのみ」で正常 | **計器の盲点。別経路で総入替していた** | v0.1.1215 |

**#5 が最も重い。** v0.1.1208 の計器が「積み上がりのみ(設計どおり)」と出したので
私は「スライド説は実測で否定された」とユーザーに報告したが、それは誤りだった。
測っていた場所が違っただけで、否定されていなかった。

---

## 各版の内容

### v0.1.1209 — グリッドの「同ユーザー計N件」が窓内の数えだと分かる表記に

`popup-entry.js` のツールチップ「一覧に同ユーザー計N件」は、実際には
直近360件の窓の中だけを数えた値。「一覧に」と書いていたため全件と誤読された。

- 窓が効いているときだけ「表示中の枠内で計N件（枠外にもある場合あり）」
- 360件に収まる配信では**従来文言とバイト一致**（後方互換）
- 到達不能だった全件走査フォールバックを import ごと撤去

★引き継ぎ資料の記述2件が誤りだったので訂正済み:
  「会場のホバーカード」→ 実際は①POPのグリッドのツールチップ /
  「集計が上限未考慮」→ 集計は正しく、真因は文言

### v0.1.1210 — LPの旧ファイル名 + 版数ゲート

LPが `nicolivelog-report-lv...` という旧名を見せており、これが
「HTMLダウンロードが無くなった」という認識の原因だった（機能は生きている）。

★あわせて**LP版数が15版ぶん取り残されていた**ことが判明。前日「LPを最新化した」と
報告した直後からまた乖離しており、手作業では再発すると分かったので
`scripts/verify-bump.mjs` に `[6]` を追加（package と LP4箇所の一致を検査）。

- **罠**: LP本文には機能説明として過去版の表記（`（v0.1.240）`等）が30箇所以上ある。
  全文スキャンでは誤検知するので「現在版を示す4箇所」だけを狙う正規表現にしてある。
  LPのその4箇所の書式を変えるとゲートが「見つからない」で赤になる

### v0.1.1211 — 速報に「popup起動から何秒後の値か」を明記

**私自身の誤読を止めるための版。**

速報の「約22秒前にpopupで取得」は【速報生成時刻からの経過】であって
【popup起動からの経過】ではない。実際には起動362ms後のスナップショットだったのに
「22秒経っても鏡が空＝取りこぼし」と誤読した。

答えは `loadShadeProbe.shadeAgeMs` として既にJSONにあったのに、人間が読む行に無かった。

- 3秒未満なら「ゼロでも正常です」と明示（鏡のflushはpublishの400ms後・幕は800ms最低表示）
- **実装中に `Number(null)===0` の罠を踏み、「値が取れなかった」を「起動0.0秒」と
  偽る不具合を作った**。テストが検出

### v0.1.1212 — 会場の幾何差が本物か測定対象ズレかを判別

実測で `幾何≠ link:128×40px(①110×38px)` が出たが、調査の結果
**①popup.html と venueBar.js の当該CSSはバイト単位で同一**だった。

真因は計器の構造。両側とも各段の**先頭タイル1枚**しか測っておらず、
meta は max-width までの収縮ボックスなので「名前が長い人ほどタイルが広い」。
①と会場で先頭の人が違えば、CSSが完全一致でも差が出る。

- 両側の計測に `tileKey`（測った当のタイルの userKey）を追加
- 3分類: `css_mismatch`(同一人物=本物) / `measured_different_people`(別人) /
  `unknown_target`(判定不能・どちらにも決めつけない)

★**これを入れずにCSSを触っていたら、正しいものを壊していた。**
  実際その直前まで「移植漏れの再発だ」と判断しかけていた。

### v0.1.1213 — 読み上げで「読まれずに消えた件数」を計測

実配信(4時間8分・記録1万件)で読み上げが破綻。ところが計器の帳尻が合わなかった:
1分に約52件届いて「読めた約6件＋間引き12件＝18件」しか説明できず、**約34件が行方不明**。

真因は `voicePlayer.js` の `if (!wav ...) { ...; continue; }`。
voicevoxClient は synthesis を8000msでタイムアウトさせ catch で null を返すが、
受け側が spokenTotal も drop も増やさずに捨てていた。

- `synthNullTotal` / `synthNullNearTimeout` を追加（時間切れかどうかを別枠で数える）
- ★あわせて `buildVoiceDiagLine` の早期returnを修正。合成が全滅すると spokenTotal が
  0のままなので、**読み上げが最も壊れているときほど診断行が丸ごと消える**構造だった

**実測結果（2026-08-01 16:26 の速報）**: `合成失敗1件(時間切れ0/その他1)`。
→ **時間切れではない失敗経路が実在する**ことを確認。大規模配信での再測が必要。

### v0.1.1214 — 映像に重なる3キャラ常駐を非表示

会場モードで配信画面の左右の縁に出ていた3キャラ（左上りんく・左下たぬ姉・右こん太）が
映像に重なり「見づらくなる」ため非表示化。額縁フレーム(v0.1.1114廃止)と同じ流儀
（フラグ1つ・モデルは残す）。

★**対象を二度取り違えた**。「りんく・こん太・たぬ姉が消えてる」という報告を
  (1)レーンの段の人 (2)段の説明文のキャラアイコン と誤解し、
  スクリーンショットで初めて正しい対象が判明した。

### v0.1.1215 — アイコンが「ちらちら変わる」現象を計測 ← **要実測**

v0.1.1208 の churn 計器が数えていたのは `rebuildStoryGrowth`（全消し再構築）だけ。
真犯人はもう一方の経路だった:

```
patchStoryGrowthIconsFromSource
  = DOM枚数は変えず【各マスの中身だけ】を今のデータで上書きする経路。
    窓(sourceOffset)がずれると i 番目のマスに別人が入る＝既存アイコンのすり替え。
    枚数不変なので churn には映らない。
```

---

## 最優先: 次のセッションで最初にやること

### v0.1.1215 のすり替え計器を読む（未実測）

**取り方に条件がある。** 3回試して3回とも読めなかったので手順を明記する。

| 条件 | 理由 |
|---|---|
| **watch ページ内**のポップアップであること | 別窓だと `viewKind:"popup"` かつ `lidMiss` で描画が起動しない |
| アイコングリッドが**実際に画面に見えている**こと | 描画されないと計器が動かない |
| popup を開いたまま**数十秒以上**置くこと | 起動0.3秒の値では全部ゼロ（v0.1.1211の警告行が出る） |

読めるとこう出る:

```
グリッド中身更新 N回 / 書換Nマス — 中身の入替Nマス=既存アイコンが別人にすり替わっています(最大Nマス/回)
```

- **入替が多い** → 推論が確定。上限到達後は窓を固定する方向で直す
- **入替0（積み上がり）** → まだ別の原因がある

★`viewKind` が `embed_watch` になっている速報でないと読めない。
  `popup` になっていたら取り直し。

---

## 未着手・未判断のまま残っているもの

1. **読み上げの体感遅延** — 破綻ではないが1〜2秒台が残る。実効上限が2に張り付くのは
   遅さの【結果】であって原因ではない（`floor(6000/ema)` が0→下限2にクランプ）。
   改善するなら並列合成か先読み深度の見直し
2. **大規模配信での読み上げ破綻** — 4時間/1万件で `合成待ち8599ms` を観測。
   v0.1.1213 の計器で「時間切れかその他か」を切り分けられる状態になった。**要再測**
3. **heavyRace** — 前回16回観測。今回は0。大規模配信でのみ出る可能性
4. **ホバーカードに発言本文を出すか** — 真因判明済み（鏡に本文が入っていない）。
   ユーザーは「ディープリサーチ＋会議で決めたい」と表明
5. **popup DOM census** — 設計と実装ハンドオフは
   `docs/handoff/diagnostic-instrument-strengthen-2-*.md` に揃っている
6. **ストア申請** — 指示あり。実機で安定を確認してからを推奨した状態
7. **「名前ありなのにゆっくり顔」** — 調査済み。真因は2つで、
   (A)匿名広告主は**nicoad APIがサムネを返さないため拡張側で解決不能**
   （既存テストが仕様として固定）(B)桁レンジ境界。
   **(A)と確定したら「バグではない」として閉じる判断が要る**
8. **診断ページの初回ロードが遅い** — 初回ロード全体が未計測と判明。
   既存計器は `refresh()` の内側だけで、328KBバンドルのパースも起動処理も含まない

---

## このセッションで確立した規律

### 計器を書いたら必ず変異で赤を確認する

**「緑だから正しい」が最も当てにならないのが計器。** 今回は全ての新規計器で
実際に壊して赤になることを確認した:

| 版 | 試した変異 | 結果 |
|---|---|---|
| 1209 | `if (false)` 前置 / ホットパスに `.filter(` 混入 | 赤 |
| 1210 | LP版数を古いまま / 旧ファイル名に戻す | 赤 |
| 1211 | 印字の呼び出しに `if (false)` 前置 | 赤 |
| 1212 | 同一/別人の分岐を潰す | 赤 |
| 1213 | 時間切れ判定を潰す | 赤 |
| 1214 | フラグを true に戻す | 赤 |
| 1215 | 入替の加算を潰す | 赤 |

### 端から端まで通す統合テストが本物の欠陥を見つけた

v0.1.1213 で「state→snapshot→印字」を本番モジュールを実importして通したところ、
**印字が空**で落ちた。`buildVoiceDiagLine` の早期returnが
「読み上げが全滅しているときほど診断行が消える」構造だったのが判明。
文字列スキャンだけでは絶対に見つからなかった。

### ホットパスに走査を持ち込まない（3層で担保）

`applyStoryGrowthIconAttributes` も `patchStoryGrowthIconsFromSource` も
セル描画ごとに走る。v0.1.1201 でここに走査を入れて拡張全体を重くした前科がある。

1. **引数をスカラーのみにする** — 配列を受け取らない＝型で走査を不可能にする
2. **既読の値だけを使う** — `sourceOffset` は既に読まれているので追加走査ゼロ
3. **退化ガードをテストに入れる** — 関数ブロック内に `querySelectorAll` /
   `.filter(` / `for (const` が無いことを断言

---

## この環境で効いた手順

```bash
npm run verify:cc          # 出荷ゲート一本
npm run copy:ext           # C:\nicolive-ext へ配布
```

- **新規 lib を足したら** `npm run tree-map` と `npm run feature-map` の
  **両方**を再生成して同じコミットに含める（片方だけだと feature-map で赤）
- **新規ファイルは `git add` してから** `verify:cc` を回す
  （`check:tracked-imports` が「未追跡ファイルへの import」で赤になる。
  これは過去に Vercel 全滅を起こしたコミット漏れを検知するゲート）
- **型定義（`@typedef`）にもフィールドを足す** — typecheck が検知する
- push 後は pre-push フックのビルドで dist の buildId が1つずれる。追わなくてよい
- `git add -A` は使わない

### ユーザーへの反映3手順（push しただけでは届かない）

1. `git pull`（こちらで実行する。ユーザーに頼まない）
2. 拡張をリロード（🔄）← **ユーザー操作が必須**
3. watch タブを F5 ← **ユーザー操作が必須**

`copy:ext` まで済ませておけば 2 と 3 だけで反映される。
